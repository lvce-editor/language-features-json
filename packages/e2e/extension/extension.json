{
  "id": "builtin.language-features-json",
  "name": "Language Features json",
  "description": "Provides rich language support for JSON files during end-to-end tests",
  "activation": [
    "onLanguage:json",
    "onCompletion:json",
    "onDiagnostic:json",
    "onTabCompletion:json",
    "onHover:json"
  ],
  "browser": "dist/languageFeaturesJsonMain.js",
  "isolated": true,
  "commands": [],
  "completionProviders": [
    {
      "id": "json.provideCompletions.json",
      "languageId": "json"
    }
  ],
  "diagnosticProviders": [
    {
      "id": "json.provideDiagnostics.json",
      "languageId": "json"
    }
  ],
  "configuration": {
    "gptvoice.tools.terminal.enabled": {
      "type": "boolean",
      "default": false,
      "description": "Allow Gpt Voice to execute Bash commands in the opened workspace."
    },
    "editor.cache": {
      "type": "boolean",
      "default": true,
      "description": "Caches editor state for faster restoration."
    },
    "simpleBrowser.openExternalLinks": {
      "type": "string",
      "enum": ["newTab", "externalBrowser"],
      "default": "newTab",
      "description": "Controls where external links are opened."
    },
    "test.boolean.false": {
      "type": "boolean",
      "default": false,
      "description": "A boolean setting with a false default for completion tests."
    },
    "test.number": {
      "type": "number",
      "default": 42,
      "description": "A number setting for completion tests."
    },
    "test.string": {
      "type": "string",
      "default": "value",
      "description": "A string setting for completion tests."
    }
  },
  "hoverProviders": [
    {
      "id": "json.provideHover.json",
      "languageId": "json"
    }
  ],
  "languages": [
    {
      "id": "json",
      "extensions": [".json"]
    }
  ]
}
